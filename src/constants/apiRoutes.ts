/**
 * object apiRoutes
 * @description we are defining all end points here  !!
 */
export const apiRoutes = {
  slash: "/",
  api: "/api",
  swagger: "/swagger",
  register: "/register",
  passwordsetup: "/passwordsetup",
  login: "/login",
  logout: "/logout",
  testapi: "/testapi",
};
